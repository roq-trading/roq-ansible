# !!! THIS FILE HAS BEEN AUTO-GENERATED !!!
{% if config_conda.services[item].conda.package == 'roq-service-manager' %}
{% for name in config_conda_user_services %}

[services.{{ name }}]
service_listen_address="unix://{{ config_user.run_dir }}/service/{{ name }}.sock"
{% endfor %}
{% elif config_conda.services[item].conda.package in ('roq-fix-bridge', 'roq-sbe-publisher') %}
{{ config_conda.services[item].config.symbols | roq_toml_symbols }}

{{ config_conda.services[item].config | dict2items | rejectattr("key", "eq", "symbols") | list | items2dict | roq_toml }}
{% else %}

{{ config_conda.services[item].config | roq_toml }}
{%- endif %}
