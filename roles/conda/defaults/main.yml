---

conda:
  url: "https://github.com/conda-forge/miniforge/releases/latest/download"
  script: 'Mambaforge-{{ "MacOSX" if ansible_os_family == "Darwin" else "Linux" }}-{{ ansible_architecture }}.sh'
  sources:
    - "conda-forge"

roq:
  url: "https://roq-trading.com"
