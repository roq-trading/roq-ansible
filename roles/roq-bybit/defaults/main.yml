---

roq_bybit_defaults:
  version: "{{ roq_version }}"
  docker:
    use: "{{ roq_use_docker }}"
    name: "roqtrading/roq-bybit"
  service:
    start: "Sun *-*-* 00:01:00"
    stop: "Sat *-*-* 23:59:00"
  logging:
    verbosity: 0
  flags:
    name: "bybit"
    config_file: "{{ directory_config.usr_local_etc }}/roq-bybit/config.toml"
    event_log_dir: "{{ directory_config.var_lib }}/roq/data"
    client_listen_address: "{{ directory_config.run }}/roq/roq-bybit.sock"
    metrics_listen_address: "{{ directory_config.run }}/roq/metrics/roq-bybit.sock"
    auth_keys_file: "{{ directory_config.usr_local_etc }}/roq/keys.json"
    auth_cache_dir: "{{ directory_config.var_lib }}"
    cache_dir: "{{ directory_config.var_lib }}/roq/cache"
    rest_uri: "https://api-testnet.bybit.com"
    ws_public_uri: "wss://stream-testnet.bybit.com/spot/quote/ws/v2"
    ws_private_uri: "wss://stream-testnet.bybit.com/spot/ws"
  symbols:
    - ".*"
