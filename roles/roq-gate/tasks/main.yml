---

- name: combine user configuration with defaults
  set_fact:
    roq_gate_config: >
        {{ roq_gate_defaults | combine(roq_gate | default({}), recursive=True) }}
    cacheable: true
  tags:
    - always

- block:

  - name: shorter variable name for config
    set_fact:
      config: "{{ roq_gate_config }}"

  - name: install
    include_role:
      name: common
      tasks_from: gateway

  when: "'roq_gate' in group_names"
  tags:
    - roq-gate
